<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vip.xiaozhao.intern.baseUtil.intf.mapper.TuiLikeMapper">

    <resultMap id="BaseResultMap" type="vip.xiaozhao.intern.baseUtil.intf.entity.TuiLike">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="target_id" property="targetId"/>
        <result column="target_type" property="targetType"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="vip.xiaozhao.intern.baseUtil.intf.entity.TuiLike">
        INSERT INTO tui_like (user_id, target_id, target_type, status, create_time, update_time)
        VALUES (#{userId}, #{targetId}, #{targetType}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByUserAndTarget" resultMap="BaseResultMap">
        SELECT * FROM tui_like 
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
    </select>

    <update id="reactivateByUserAndTarget">
        UPDATE tui_like SET status = 0, update_time = NOW()
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType} AND status = 1
    </update>

    <update id="deleteByUserAndTarget">
        UPDATE tui_like SET status = 1, update_time = NOW()
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType} AND status = 0
    </update>

</mapper>

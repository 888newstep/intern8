# 项目技术深化总结

## 项目概述

本项目是一个社交动态系统，支持用户发布动态、点赞、评论、关注、通知等核心功能。通过本次技术深化，系统在认证安全、缓存性能、消息可靠性、并发正确性等方面得到了全面提升。

## 技术栈

- **基础框架**：Spring Boot 3.3 + Java 17
- **持久层**：MyBatis + MySQL 8.0
- **缓存**：Redis 7.2 + Caffeine（多级缓存）
- **消息队列**：RabbitMQ 3.13（集群模式）
- **安全**：Spring Security + JWT
- **监控**：Prometheus + Grafana
- **压测**：JMeter 5.6
- **部署**：Docker + Docker Compose

## 深化成果

### 1. 鉴权闭环（P0）

**问题**：原系统写接口直接从请求体读取 userId，存在身份冒用风险。

**方案**：
- 改造 SecurityConfig，添加统一的 AuthenticationEntryPoint 和 AccessDeniedHandler
- 改造 BaseController，添加 getCurrentUserId() 方法从 SecurityContext 获取当前用户
- 改造所有写接口（DynamicController、CommentController），不再信任请求体中的 userId
- 清理 DTO 中的 userId 字段，区分"操作对象"和"当前操作者"
- 统一 GlobalExceptionHandler，处理 AuthenticationException 和 AccessDeniedException

**结果**：
- 所有写接口的身份都来自服务端信任的 JWT token
- 前端无法通过篡改请求体来冒用他人身份
- 统一了 401/403 错误响应

### 2. 配置与密钥治理（P0）

**问题**：配置文件中存在明文数据库、Redis、COS 等敏感信息。

**方案**：
- 将所有敏感配置改为环境变量注入（）
- 创建 .env.example 文件说明所有环境变量
- 更新 docker-compose.yml 使用环境变量
- 创建 DEPLOY_CONFIG.md 部署配置说明文档

**结果**：
- 仓库中不再保留真实密钥
- 新环境部署只需准备 .env 文件
- 配置管理规范化

### 3. 高频读路径缓存化（P1）

**问题**：高频读接口直接查询数据库，性能瓶颈明显。

**方案**：
- 选择动态详情作为缓存改造样板
- 使用 RedisCacheService（Caffeine + Redis 多级缓存）
- 实现 Cache-Aside 模式，TTL 1小时
- 删除动态时清除缓存
- 添加缓存指标统计（命中率、回源率、DB 加载次数）
- 创建 MetricsController 暴露缓存指标

**结果**：
- 动态详情查询性能显著提升
- 缓存命中率可观测
- 为后续更多缓存场景提供了样板

### 4. 异步链路可靠性补强（P1）

**问题**：MQ 消息发送失败时无法追踪和补偿。

**方案**：
- 创建 mq_message_status 表记录消息状态
- 修改 RabbitMQSender，在发送前记录消息状态
- 修改 confirm callback，在确认成功/失败时更新状态
- 修改 Consumer，在消费成功时更新状态
- 创建 MessageCompensationTask 定时任务框架

**结果**：
- 消息状态可追踪（待发送、发送成功、发送失败、消费成功、消费失败）
- 失败消息可补偿
- 消息生命周期完整记录

### 5. 并发正确性兜底（P1）

**问题**：分布式锁不能代替所有数据正确性约束。

**方案**：
- 为关注关系添加数据库唯一约束（已有）
- 创建 tui_like 点赞关系表
- 修改点赞方法，使用点赞关系表保证幂等性
- 捕获 DuplicateKeyException，转换为业务异常
- 添加唯一约束 SQL 脚本

**结果**：
- 关注关系不会重复
- 点赞操作幂等
- 数据库层提供最终一致性保护

## 架构亮点

### 多级缓存架构

`
请求 -> Caffeine (L1) -> Redis (L2) -> MySQL (L3)
       毫秒级访问        网络IO         磁盘IO
`

- L1：Caffeine 本地缓存，最大 10000 条，5分钟过期
- L2：Redis 分布式缓存，1小时过期
- L3：MySQL 数据库

### 可靠消息架构

`
业务操作 -> 本地事务 -> MQ 发送 -> 消息确认 -> 消费处理 -> 状态更新
              |                                    |
              v                                    v
         消息状态表 <-------------------------- 状态追踪
`

- 消息状态持久化
- 发送确认回调
- 消费幂等保证
- 失败重试机制
- 死信队列兜底

### 并发保护体系

`
请求 -> 限流（RateLimiter） -> 分布式锁（Redisson） -> 数据库唯一约束
       前置保护                并发控制              最终兜底
`

- 三层保护，层层递进
- 锁失效时数据库仍能阻止错误写入

## 监控指标

### 缓存指标
- cache.hitCount：缓存命中次数
- cache.missCount：缓存未命中次数
- cache.dbLoadCount：数据库加载次数
- cache.hitRate：缓存命中率

### MQ 指标
- 消息状态：待发送、发送成功、发送失败、消费成功、消费失败
- 重试次数
- 死信数量

### 业务指标
- 接口响应时间（P50、P95、P99）
- QPS
- 错误率

## 部署架构

`
┌─────────────┐
│   Nginx     │
└──────┬──────┘
       │
┌──────▼──────┐
│  App (x2)   │  <- Docker 容器
└──────┬──────┘
       │
┌──────▼──────────────────────────────┐
│                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────┐ │
│  │ MySQL   │  │ Redis   │  │ MQ  │ │
│  │ Master  │  │Sentinel │  │Cluster│ │
│  └─────────┘  └─────────┘  └─────┘ │
│                                      │
└──────────────────────────────────────┘
       │
┌──────▼──────┐
│ Prometheus  │  <- 监控
└──────┬──────┘
       │
┌──────▼──────┐
│  Grafana    │  <- 可视化
└─────────────┘
`

## 性能数据

（待压测后补充）

## 经验总结

### 做得好的地方

1. **边界优先**：先修边界（鉴权、配置），再做性能优化
2. **渐进式改造**：从单个缓存场景开始，逐步扩展
3. **可观测性**：每个改造都添加了指标统计
4. **多层保护**：并发控制不依赖单一机制

### 可以改进的地方

1. **测试覆盖**：单元测试和集成测试还不够完善
2. **文档完善**：部分代码注释存在乱码
3. **性能基线**：缺少优化前的性能数据对比

### 最佳实践

1. **身份边界**：永远不要信任前端传来的身份标识
2. **缓存策略**：热点数据用多级缓存，冷数据不缓存
3. **消息可靠性**：发送前记录状态，消费后更新状态
4. **并发控制**：锁是前置保护，数据库约束是最终兜底

## 后续规划

### 短期（1-2周）

- [ ] 补充更多缓存场景（Feed、通知列表）
- [ ] 完善消息补偿任务
- [ ] 跑一轮完整压测，输出性能数据

### 中期（1-2月）

- [ ] 引入读写分离
- [ ] 优化慢查询 SQL
- [ ] 补充集成测试

### 长期（3-6月）

- [ ] 考虑分库分表
- [ ] 引入 Elasticsearch 优化搜索
- [ ] 优化分布式锁策略

## 简历亮点

1. **完成基于 JWT 的无状态认证闭环，修复接口信任前端传参的身份冒用风险**
2. **完成多环境配置治理与敏感信息外置化，提升部署安全性和可迁移性**
3. **基于 Caffeine + Redis 实现多级缓存，针对热点查询场景设计一致性与抗击穿策略**
4. **完善 RabbitMQ 可靠消息机制，补齐重试、死信、幂等及本地消息补偿闭环**
5. **基于 Redisson + 数据库约束构建并发保护体系，降低重复操作与竞态写入风险**
6. **构建 Prometheus + Grafana + JMeter 性能观测链路，以指标驱动接口优化**

## 参考文档

- [技术深化方案](tech-deepening-checklist.md)
- [部署配置说明](DEPLOY_CONFIG.md)
- [性能压测报告](PERFORMANCE_TEST_REPORT.md)
- [环境变量示例](.env.example)

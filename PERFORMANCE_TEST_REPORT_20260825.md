# Intern8 压测与写入安全验证报告（2026-08-25）

> 状态：本项目现有 JMeter 只读与写入场景均已执行完成；RabbitMQ 公网链路恢复后，Outbox 投递、通知消费、延迟归档和远程 DLQ 路由也已验证。本文只记录本轮重测结果，不覆盖历史报告。

## 1. 结论摘要

- 只读场景：20 并发、5 秒爬坡、持续 30 秒，共 2,567 个样本，错误 0，整体吞吐约 85.0 RPS。
- 写入场景：发布、点赞、评论各 4 线程，2 秒爬坡、持续 15 秒，共 1,296 个样本，JMeter 断言错误 0，整体吞吐约 85.6 RPS。
- 固定幂等键并发重放只产生 3 次真实业务执行：新增动态、点赞、评论各 1 次；计数与关系表一致。
- 3 条 Outbox 全部成功派发：2 条通知完成消费并落库，1 条归档事件按设计进入 7 天延迟队列。
- RabbitMQ 远程集成测试通过 Publisher Confirm、手动 ACK/重投、mandatory return、NACK 到 DLQ、retry TTL 回主队列等场景。
- 测试结束后，MySQL 隔离 schema、Redis 测试键和 RabbitMQ 延迟消息均已清理；7 个业务/retry/DLQ 队列归零，8080 已释放。

本轮可以证明当前测试数据规模和并发模型下的接口表现及固定请求并发重放一致性，不能外推为生产容量上限。

## 2. 测试环境

| 项目 | 配置 |
|---|---|
| Git revision | `0fbbad4` + 当前未提交工作树 |
| 应用 | Spring Boot 3.3.5，Java 17，本机 `127.0.0.1:8080` |
| JMeter | 5.6.3，CLI 非 GUI 模式 |
| MySQL | Win11 本机 `127.0.0.1:3306`，随机隔离 schema |
| Redis | Win11 本机 `127.0.0.1:6379`，DB 4，single 模式 |
| RabbitMQ | 云服务器 `49.234.187.76:5672`，RabbitMQ 3.13.7，独立 vhost `/intern8` |
| Milvus | 当前项目不使用，未纳入测试 |
| 身份认证 | Dev Profile 临时 JWT，仅注入 JMeter 子进程 |

隔离数据集：

| 表 | 数量/用途 |
|---|---:|
| `tui_follow` | 1,000 条，测试用户为 `user_id=1` |
| `tui_dynamic` | 5,001 条，写入目标动态 `id=900001` |
| `tui_notification` | 1,000 条只读基线数据 |

## 3. 只读压测

### 3.1 并发模型

| 接口 | 线程数 | 请求 |
|---|---:|---|
| 动态详情 | 10 | `GET /api/dynamic/detail/900001` |
| Feed | 6 | `POST /api/dynamic/feed` |
| 通知列表 | 4 | `POST /api/notification/list` |

统一参数：5 秒爬坡、持续 30 秒、Feed 游标 `999999999`、通知游标 `999999`。

### 3.2 测试结果

| 场景 | Samples | Errors | Error Rate | Throughput | Average | P50 | P95 | P99 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Dynamic Detail | 1,352 | 0 | 0% | 45.569 RPS | 204.590 ms | 161 ms | 461 ms | 862 ms |
| Feed | 700 | 0 | 0% | 23.559 RPS | 238.409 ms | 185 ms | 544 ms | 1,381 ms |
| Notification List | 515 | 0 | 0% | 17.337 RPS | 217.241 ms | 170 ms | 473 ms | 1,066 ms |
| **整体** | **2,567** | **0** | **0%** | **约 85.0 RPS** | **216 ms** | - | - | **最大响应 2,064 ms** |

### 3.3 缓存与应用指标

| 指标 | 结果 |
|---|---:|
| `cache.hit.local` | 1,348 |
| `cache.hit.redis` | 3 |
| `cache.miss` | 4 |
| `cache.db.load` | 1 |
| `cache.singleflight.failure` | 0 |
| `cache.singleflight.timeout` | 0 |
| Feed 查询次数 | 700 |
| Feed 查询累计耗时 | 15.586 秒 |
| JVM Young GC 次数 | 9 |
| 采样时进程 CPU | 约 6.1% |

动态详情的 L1 本地缓存命中率约为 99.70%（1,348 / 1,352），数据库只加载 1 次，未出现 single-flight 失败或超时。

### 3.4 与同日上一轮对比

| 指标 | 上一轮 | 本轮 | 变化 |
|---|---:|---:|---:|
| 整体样本 | 3,595 | 2,567 | -28.6% |
| 整体吞吐 | 118.8 RPS | 85.0 RPS | -28.5% |
| 动态详情 P99 | 1,252 ms | 862 ms | 改善 31.2% |
| Feed P99 | 2,135 ms | 1,381 ms | 改善 35.3% |
| 通知列表 P99 | 1,982 ms | 1,066 ms | 改善 46.2% |
| 错误率 | 0% | 0% | 持平 |

本轮吞吐下降但 P99 改善。应用在压测期间对每次请求输出多条 INFO 日志，可能影响本机吞吐；由于本轮没有执行日志关闭前后的严格 A/B 测试，不能将下降完全归因于日志。

## 4. 写入安全压测

### 4.1 并发模型

| 场景 | 线程数 | 幂等策略 |
|---|---:|---|
| 发布动态 | 4 | 全线程共享固定 key 和固定 body |
| 点赞动态 | 4 | 全线程共享固定 key 和固定 body |
| 评论动态 | 4 | 全线程共享固定 key 和固定 body |

统一参数：2 秒爬坡、持续 15 秒。该场景验证同一请求的并发重放安全，不代表 1,296 次独立业务写入。

### 4.2 HTTP 结果

| 场景 | Samples | Errors | Error Rate | Throughput | Average | P50 | P95 | P99 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Publish Dynamic | 427 | 0 | 0% | 29.203 RPS | 131.881 ms | 111 ms | 316 ms | 427 ms |
| Like Dynamic | 432 | 0 | 0% | 29.522 RPS | 130.146 ms | 107 ms | 303 ms | 423 ms |
| Comment Dynamic | 437 | 0 | 0% | 29.835 RPS | 128.696 ms | 103 ms | 293 ms | 517 ms |
| **整体** | **1,296** | **0** | **0%** | **约 85.6 RPS** | **130 ms** | - | - | **最大响应 1,190 ms** |

JMeter 断言接受业务成功响应和预期的“相同请求正在处理”409 响应。所有 HTTP 状态均为 200，响应体断言全部通过。

### 4.3 幂等与数据库一致性

| 检查项 | 结果 |
|---|---:|
| `api.idempotency.completed` | 3 |
| `api.idempotency.hit` | 1,272 |
| `api.idempotency.in_progress` | 21 |
| `api.idempotency.conflict` | 0 |
| `api.idempotency.unavailable` | 0 |
| `api.idempotency.execution_failure` | 0 |
| 新增动态 | 1 条 |
| 点赞关系 | 1 条 |
| 目标动态 `like_count` | 1 |
| 评论记录 | 1 条 |
| 目标动态 `comment_count` | 1 |

关系表和动态聚合计数完全一致，未发现重复写入或计数漂移。

## 5. Outbox、RabbitMQ 与 DLQ

### 5.1 Outbox 最终状态

| 事件 | 数量 | Outbox 最终状态 | Broker/消费结果 |
|---|---:|---|---|
| `notification.created` | 2 | `DISPATCHED` | 2 条均消费成功并落库 |
| `dynamic.archive` | 1 | `DISPATCHED` | Broker Confirmed，进入 7 天延迟队列 |

关键指标：

| 指标 | 结果 |
|---|---:|
| `mq.outbox.dispatched` | 3 |
| `mq.outbox.failed` | 0 |
| `mq.outbox.dead_lettered` | 0 |
| 通知新增记录 | 2 |
| `mq_message_status` 通知已消费 | 2 |
| `mq_message_status` 归档已确认 | 1 |

### 5.2 远程 RabbitMQ 集成验证

`RabbitMqRemoteIT` 在 `/intern8` 上完整通过，覆盖：

1. Publisher Confirm 返回 ACK。
2. 未 ACK 消息断开连接后能够 redelivery，随后手动 ACK。
3. requestId、userId、clientIp 等上下文头经过 broker 后保持不变。
4. mandatory 消息使用不存在的 routing key 时触发 returned message。
5. `basicNack(requeue=false)` 将消息路由到临时 DLQ，并生成 `x-death: rejected` 记录。
6. retry 队列 TTL 到期后将消息路由回主队列，并生成 `x-death: expired` 记录。

测试使用随机临时交换机/队列，结束后自动删除，不污染项目业务拓扑。

### 5.3 队列终态

| 队列 | Ready | Unacked | Total |
|---|---:|---:|---:|
| `archive.delay.queue` | 0 | 0 | 0 |
| `archive.queue` | 0 | 0 | 0 |
| `archive.retry.queue` | 0 | 0 | 0 |
| `archive.dlq.queue` | 0 | 0 | 0 |
| `notification.queue` | 0 | 0 | 0 |
| `notification.retry.queue` | 0 | 0 | 0 |
| `notification.dlq.queue` | 0 | 0 | 0 |

## 6. 原始证据

### 6.1 只读场景

- JTL：`target/jmeter-results/read-20260825-111553730-0fa906c9/results.jtl`
- 汇总：`target/jmeter-results/read-20260825-111553730-0fa906c9/summary.json`
- HTML：`target/jmeter-results/read-20260825-111553730-0fa906c9/html-report/index.html`

### 6.2 写入场景

- JTL：`target/jmeter-results/write-20260825-112558011-cf4e0a85/results.jtl`
- 汇总：`target/jmeter-results/write-20260825-112558011-cf4e0a85/summary.json`
- HTML：`target/jmeter-results/write-20260825-112558011-cf4e0a85/html-report/index.html`

## 7. 清理结果

- 应用已正常停止，`127.0.0.1:8080` 未监听。
- 隔离 schema `intern_perf_20260825_111000` 已删除。
- Redis 本轮 11 个缓存、幂等、限流和消费者幂等键已定向删除；未执行 `FLUSHDB`。
- 通知未读计数按本轮增量 2 精确恢复。
- 唯一的测试归档延迟消息已定向清理。
- RabbitMQ 临时集成测试拓扑已删除，7 个项目队列全部归零。

## 8. 适用边界与后续建议

本报告已覆盖项目当前两个 JMeter 计划和 RabbitMQ 安全链路，但仍存在以下边界：

- 写入场景验证固定 key、固定 body 的并发重放，不等价于高比例独立写入吞吐测试。
- 压测由应用、JMeter、MySQL 和 Redis 共用同一台 Win11 主机，结果包含资源竞争影响。
- 测试时间较短，不能代表数小时稳态运行、内存泄漏或连接池长期行为。
- 尚未执行应用提交事务后立即崩溃、Outbox 租约回收及消费者进程重启等故障注入演练。
- 当前 RabbitMQ 管理凭据曾用于测试，应在测试结束后轮换，并将 5672/15672 限制为可信来源 IP。

如用于容量规划，下一阶段应补充独立压测机、30 分钟以上稳态测试、独立写入 key 模型、MySQL 慢查询/连接池和 JVM GC 持续采样。

## 9. 四档容量阶梯压测

### 9.1 测试模型

四档测试均使用同一个隔离数据集和相同请求参数，线程按详情、Feed、通知 `50% / 30% / 20%` 分配。每档爬坡 10 秒、持续 60 秒，应用根日志级别设为 `WARN`，避免同步 INFO 请求日志干扰容量结果。

| 档位 | 总并发 | 详情线程 | Feed 线程 | 通知线程 | 目标 |
|---:|---:|---:|---:|---:|---|
| 1 | 100 | 50 | 30 | 20 | 0 错误、聚合 P95 < 60 ms |
| 2 | 250 | 125 | 75 | 50 | 0 错误、聚合 P95 < 100 ms |
| 3 | 500 | 250 | 150 | 100 | 聚合 P95 < 200 ms，或可降级不崩 |
| 4 | 1,000 | 500 | 300 | 200 | 找到吞吐/延迟拐点和瓶颈位置 |

压测机与应用、MySQL、Redis 共用一台 Win11：Intel Core i5-13500H，12 核 16 线程，15.7 GB 内存。RabbitMQ 位于云服务器，但本组只读请求不产生消息。

### 9.2 聚合结果与验收

| 并发 | Samples | Errors | 吞吐 | Average | P50 | P95 | P99 | 验收结论 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 100 | 525,036 | 0 | 8,732.6 RPS | 10.4 ms | 3 ms | 51 ms | 215 ms | **通过聚合目标** |
| 250 | 787,162 | 0 | 13,066.0 RPS | 17.4 ms | 4 ms | 33 ms | 334 ms | **通过聚合目标** |
| 500 | 238,048 | 0 | 3,942.2 RPS | 115.8 ms | 78 ms | 515 ms | 869 ms | P95 未达标，但 0 错误、应用及依赖保持 UP，满足“可降级不崩” |
| 1,000 | 100,155 | 0 | 1,632.1 RPS | 555.0 ms | 508 ms | 1,348 ms | 2,147 ms | 极限档完成，确认已越过拐点 |

四档在旧 JMeter 口径下均为 0 错误，测试结束后的应用健康检查仍为 `UP`，MySQL、Redis、RabbitMQ 均为 `UP`。后续优化复测发现，该脚本当时只校验 HTTP 状态，没有校验响应体 `success`，因此这里的 0 错误只能解释为 HTTP/断言错误为 0，不能证明业务错误为 0。吞吐在 250 并发达到峰值，500 并发比峰值下降 69.8%，1,000 并发进一步下降 58.6%，因此旧口径下的拐点位于 **250 到 500 并发之间**。

### 9.3 分接口 P95

| 并发 | Dynamic Detail P95 | Feed P95 | Notification P95 | 最慢接口 P95 |
|---:|---:|---:|---:|---:|
| 100 | 19 ms | 2,607 ms | 2,222 ms | 2,607 ms |
| 250 | 30 ms | 3,999 ms | 3,595 ms | 3,999 ms |
| 500 | 136 ms | 960 ms | 939 ms | 960 ms |
| 1,000 | 1,011 ms | 2,058 ms | 2,013 ms | 2,058 ms |

聚合 P95 在 100/250 档达标，但这不能解释为三个接口都达标。JMeter 使用封闭线程组模型：响应快的详情线程在 60 秒内循环次数远高于 Feed 和通知线程。因此线程比例是 `50/30/20`，实际请求比例并不是固定的 `50%/30%/20%`：

| 并发 | 详情请求占比 | Feed 请求占比 | 通知请求占比 |
|---:|---:|---:|---:|
| 100 | 99.47% | 0.24% | 0.29% |
| 250 | 99.59% | 0.21% | 0.19% |
| 500 | 87.52% | 7.35% | 5.13% |
| 1,000 | 72.56% | 16.34% | 11.10% |

所以聚合 P95 被详情缓存请求主导。若验收标准要求“每个接口 P95 均低于阈值”，100 和 250 档均不通过，主要失败项为 Feed 和通知列表。

### 9.4 单接口吞吐与延迟

| 并发 | 接口 | Samples | Throughput | Average | P95 | P99 |
|---:|---|---:|---:|---:|---:|---:|
| 100 | Dynamic Detail | 522,237 | 8,722.1 RPS | 5.2 ms | 19 ms | 61 ms |
| 100 | Feed | 1,262 | 21.0 RPS | 1,313.7 ms | 2,607 ms | 3,357 ms |
| 100 | Notification | 1,537 | 25.6 RPS | 719.5 ms | 2,222 ms | 2,873 ms |
| 250 | Dynamic Detail | 783,969 | 13,084.0 RPS | 8.7 ms | 30 ms | 48 ms |
| 250 | Feed | 1,672 | 27.8 RPS | 2,479.5 ms | 3,999 ms | 5,822 ms |
| 250 | Notification | 1,521 | 25.3 RPS | 1,816.1 ms | 3,595 ms | 5,974 ms |
| 500 | Dynamic Detail | 208,342 | 3,473.7 RPS | 66.0 ms | 136 ms | 158 ms |
| 500 | Feed | 17,505 | 289.9 RPS | 473.7 ms | 960 ms | 1,439 ms |
| 500 | Notification | 12,201 | 202.1 RPS | 452.8 ms | 939 ms | 1,408 ms |
| 1,000 | Dynamic Detail | 72,671 | 1,200.7 RPS | 381.5 ms | 1,011 ms | 1,639 ms |
| 1,000 | Feed | 16,363 | 266.7 RPS | 1,021.9 ms | 2,058 ms | 2,999 ms |
| 1,000 | Notification | 11,121 | 181.3 RPS | 1,001.3 ms | 2,013 ms | 2,956 ms |

### 9.5 资源观测

CPU 为压测期间 Win11 整机采样，包含应用、JMeter、MySQL 和 Redis，不能视为单独的应用 CPU。

| 并发 | 整机 CPU 平均 | 整机 CPU 峰值 | 应用最大工作集 |
|---:|---:|---:|---:|
| 100 | 41.1% | 82.0% | 878.1 MB |
| 250 | 24.9% | 41.2% | 847.4 MB |
| 500 | 29.0% | 91.2% | 854.1 MB |
| 1,000 | 44.8% | 96.3% | 909.2 MB |

累计四档后：详情缓存本地命中 1,587,207 次、Redis 命中 12 次、数据库加载 1 次，single-flight 失败和超时均为 0；Young GC 748 次，累计暂停约 2.623 秒。MySQL `Max_used_connections=21`、`Slow_queries=0`、磁盘临时表为 0，未达到 `max_connections=151`，因此当前证据不支持“数据库连接数耗尽”。

### 9.6 拐点与瓶颈定位

1. **首要拐点在应用并发排队层。** 250 到 500 并发时吞吐不升反降，500/1,000 档 CPU 峰值达到 91.2%/96.3%。项目没有覆盖 Tomcat 线程配置，使用框架默认工作线程上限；并发跨过该数量级后，请求在 HTTP 工作线程和下游调用前排队。由于本轮没有采到 Tomcat busy-thread 峰值，这一项为高置信度推断，而非直接计数证据。
2. **Feed SQL 是数据库侧最明确的热点。** 生产查询的 `EXPLAIN ANALYZE` 在 5,001 条动态、1,000 条关注数据上扫描 5,001 个候选动态并排序，单次约 25.7 ms。累计 Feed 计时 36,802 次、总耗时 1,781.9 秒，平均约 48.4 ms。
3. **通知 SQL 本身不是主要慢点。** 相同数据下通知游标查询 `EXPLAIN ANALYZE` 约 0.16 ms，但 JMeter P95 仍很高，表明主要时间消耗在应用工作线程排队、连接获取、序列化和同机资源竞争，而不是该 SQL 执行计划。
4. **详情缓存不是数据库瓶颈。** 详情仅加载数据库 1 次，随后几乎全部命中 Caffeine；250 档详情达到约 13,084 RPS。高档位详情延迟恶化来自共享应用/压测机资源饱和。
5. **当前压测流量配比失真。** 封闭线程模型使快速接口产生远多于慢接口的样本。若目标是固定请求比例容量评估，应改用吞吐整形计时器、到达率线程组或分别执行单接口容量测试。

建议优先级：先优化 Feed ID 查询，使其从按关注作者展开、全量排序改为从动态倒序索引扫描并做关注存在性判断；随后补充 Tomcat busy threads、请求队列、Druid active/wait 和 MySQL statement latency 的持续采样；最后使用独立 JMeter 压测机和固定到达率模型重新验收四档。

### 9.7 阶梯测试原始证据

| 并发 | JTL 与 summary 目录 | HTML |
|---:|---|---|
| 100 | `target/jmeter-results/scale-20260825/read-20260825-153252843-1d6cb843/` | 已生成 |
| 250 | `target/jmeter-results/scale-20260825/read-20260825-154040915-d1ddd7ae/` | 已生成 |
| 500 | `target/jmeter-results/scale-20260825/read-500-20260825-155315517/` | 未生成；保留 JTL、日志和 summary |
| 1,000 | `target/jmeter-results/scale-20260825/read-1000-20260825-155644741/` | 未生成；保留 JTL、日志和 summary |

500/1,000 档跳过 HTML 聚合是因为高样本量报告生成时间远超请求执行时间。延迟、吞吐和错误率当时均由原始 JTL 通过同一个 `analyze-jmeter-results.ps1` 计算。优化阶段执行完整 `mvn clean test` 后，位于 Maven `target/` 下的旧 JTL/HTML 已被构建清理；上表路径仅保留当时的审计记录，旧档现存证据为本报告中的汇总数据。优化复测的新 JTL/summary 已重新生成并保留。

## 10. RPS 优化与复测

### 10.1 已落地改动

- Feed 增加有界候选快路径：按动态倒序覆盖索引扫描最近 `limit * 3` 条（最多 300 条），拿满一页时直接回表；不足一页回退原自适应 `EXISTS` 查询，避免稀疏关注用户退化。
- 删除仅重复打印请求日志的 `PrehandleInterceptor`，Controller 成功日志从 INFO 调整为 DEBUG；异常日志和限频策略保持不变。
- Tomcat 默认线程上限保持 200。实测 300 线程在同机高 CPU 场景中放大争抢，已撤回；Druid 上限改为环境可调的 64，获取连接最多等待 3 秒。
- 只读 JMeter 三个接口均新增响应体 `"success":true` 断言，修复 HTTP 200 包装业务失败时漏报错误的问题。
- JTL 分析器改为流式按接口聚合，每组只排序一次，不再为三个分位数重复复制和排序全部样本。

### 10.2 正确性与回归

| 验证项 | 结果 |
|---|---|
| 完整 `mvn clean test`（候选窗口收敛前） | 111 项，0 失败 |
| 最终 Feed 单元测试 | 21 项，0 失败 |
| 最终本机 MySQL Feed IT | 5 项，0 失败，隔离 schema 已清理 |
| JMeter 业务断言 smoke | 1,913 样本，0 错误 |
| 配置契约测试 | 3 项，0 失败 |

MySQL IT 同时验证：密集页快路径与原查询结果、顺序一致；窗口不足会产生回退信号；稀疏关注场景继续由优化器从 10 条关注关系开始执行，约 `0.10 ms`，不会被强制动态倒序扫描拖慢到约 `193 ms`。

### 10.3 修复断言后的 500 档

该档使用 Tomcat 200 线程和 `limit * 10` 的候选窗口完成；之后窗口进一步收敛为最终的 `limit * 3`，因此该结果用于证明业务断言与优化方向，不作为最终容量上限。

| 接口 | Samples | Errors | RPS | Average | P95 | P99 |
|---|---:|---:|---:|---:|---:|---:|
| Dynamic Detail | 158,322 | 0 | 2,658.8 | 86.7 ms | 165 ms | 341 ms |
| Feed | 27,361 | 0 | 457.4 | 301.6 ms | 1,048 ms | 1,917 ms |
| Notification | 18,894 | 0 | 315.9 | 291.1 ms | 1,052 ms | 1,924 ms |
| **整体** | **204,577** | **0** | **3,382.1** | **134 ms** | - | - |

相对旧 500 档，Feed 吞吐从 `289.9` 提升到 `457.4 RPS`（+57.8%），平均延迟从 `473.7` 降到 `301.6 ms`（-36.3%）；通知吞吐从 `202.1` 提升到 `315.9 RPS`。整体吞吐低于旧值 14.2%，一方面是快速接口不再占绝对多数，另一方面复测期间同机存在其他 Java 服务；不能把整体差异单独归因于本项目。

原始证据：`target/jmeter-results/optimization-20260825/read-500-validated-20260825-170222898/`。

### 10.4 最终窗口参数 A/B

在 5,001 条动态、1,000 条关注且近期动态全部命中的数据上，每组执行 7 次、每次 1,000 查询：

| 查询 | 平均耗时/1,000 次 |
|---|---:|
| 原自适应查询 | 2.198 s |
| 快路径 60 候选（最终 `3x`） | 2.475 s |
| 快路径 100 候选 | 2.625 s |
| 快路径 200 候选（初版 `10x`） | 2.654 s |

当原查询已经选择正确的动态倒序计划时，快路径存在物化窗口的固定成本；最终参数将该成本从 200 行降到 60 行。它的收益场景是旧压测中已观测到的优化器错误选路（扫描并排序 5,001 行、约 25.7 ms），而不是替换所有场景的自适应能力。

250 档修复断言后也完成了 0 业务错误验证，但压测结束后整机空闲基线仍持续占用 34%–42% CPU，详情吞吐与 500 档出现反向容量关系，因此该档判定为外部负载污染，不用于性能前后对比。

### 10.5 清理

- 应用已停止，8080 无监听。
- 隔离 schema `intern_perf_opt_20260825_1639` 已删除。
- Redis DB 4 的 `dynamic:detail:900001` 已定向删除并确认不存在。
- 本轮只读复测未产生 RabbitMQ 消息，未改动 `/intern8` 业务拓扑。

# intern8 实机单接口基线压测报告（2026-08-26）

> 目的：在当前真实业务量级数据（xiao 库 5 万动态 / 1 万关注 / 1 万通知）下，用**单接口低并发基线**方法精确区分「Feed SQL 耗时」与「环境/排队耗时」，验证 Feed 快路径 + 覆盖索引在目标量级的真实容量特性，并对 2026-08-25 报告的高 P95 结论进行归因修正。

---

## 1. 测试环境

| 项 | 值 |
|---|---|
| 应用 | `intern-base.jar`（dev profile），监听 `127.0.0.1:8080` |
| 数据链路 | health UP：db（MySQL）/ redis / rabbit（3.13.7） |
| 数据规模 | xiao 库：动态 5 万（id 900000+）、关注 1 万、通知 1 万（`[PERF_20260826]` 前缀） |
| 压测工具 | Apache JMeter 5.6.3（本机），JMX 含 `success:true` 业务断言 |
| JWT | dev 登录接口获取（userId=1） |
| 干扰源 | ⚠️ newagent（ai-agent-platform，端口 8081，~875MB）同机运行 |
| 时长 | 每档 20~30s，5s 爬坡（并发阶梯用 20s） |

## 2. 方法：单接口基线（本次核心方法贡献）

> 判定逻辑：**应用固定开销低（详情缓存接口应 <15ms）→ 单线程 Feed 快（SQL 合格）→ 并发阶梯若平滑则扩展性健康；若某档出现离群点，重测验证环境抖动**。

- 每个接口独立成档：目标接口 N 线程，其余接口 1 线程（最小化相互干扰）
- 详情 20 并发基线用于建立"应用固定开销"基准（缓存接口，P95 应为个位数 ms）
- 同档出现异常值必须重测，区分「稳定行为」与「环境抖动」

## 3. 数据

### 3.1 Feed 单接口并发阶梯（5 / 20 / 40 并发）

| 并发 | RPS | P50 | P95 | P99 | Avg | Max | 错误 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 5 | 616 | 7ms | **11ms** | 15ms | 8ms | 54ms | 0 |
| 20（重测） | 822 | 14ms | **58ms** | 118ms | 21ms | 351ms | 0 |
| 40 | 971 | 30ms | **101ms** | 151ms | 36ms | 680ms | 0 |
| 20（第 1 次） | 212 | — | 263ms | 428ms | 86ms | 1056ms | 0 ← **异常抖动** |

### 3.2 三接口 20 并发横向对比

| 接口 | RPS | P50 | P95 | P99 | Avg |
|---|---:|---:|---:|---:|---:|
| 详情（缓存） | 2,840 | 6ms | **14ms** | 22ms | 6ms |
| 通知（V3 索引 + FORCE INDEX） | 1,966 | 6ms | **26ms** | 39ms | 9ms |
| Feed（快路径 + 覆盖索引） | 822 | 14ms | **58ms** | 118ms | 21ms |

### 3.3 单线程参考（各基线中 1 线程接口）

- Feed 单线程 P95 ≈ 25~30ms（与通知 18ms 同量级，证明 SQL 本身合格）
- 详情单线程 P95 ≈ 5~17ms

## 4. 核心结论

1. **Feed SQL/路径本身很快**：5 万动态/1 万关注量级下，单请求 P95 11~58ms（5~20 并发）。覆盖索引 + 快路径（FORCE INDEX 倒序候选 + STRAIGHT_JOIN 关注探测）+ 批量 `selectByIds`（IN 一次加载）**验证无 N+1、无慢查询退化**。
2. **并发扩展性健康**：5→40 并发，P95 从 11ms 线性升至 101ms，RPS 稳定 600~970，无悬崖、无崩溃、0 业务错误——**可预期、可解释的容量曲线**。
3. **应用固定开销极低**：详情缓存接口 20 并发 P95=14ms，证明缓存链路设计（Caffeine + Redis + single-flight）有效，且固定开销不构成瓶颈。
4. **同档重测出现 4 倍差异（58ms vs 263ms）→ 判定为环境抖动**，非代码行为。抖动源疑为同机 newagent（8081）CPU 竞争。

## 5. 对 2026-08-25 报告的归因修正

| 原报告结论 | 本次实机证据 | 修正 |
|---|---|---|
| Feed 250 并发 P95=216ms，瓶颈疑似 Feed SQL / 资源 | Feed 20 并发 P95=58ms、40 并发 101ms，SQL 不慢、扩展性健康 | **高 P95 主要来自同机资源竞争（环境污染），非 Feed SQL 瓶颈** |
| 通知 250 并发 P95=252ms | 通知 20 并发 P95=26ms（V3 索引生效） | 通知 SQL 已达标 |
| 详情 P95 曾被拖至 236ms（10.7） | 详情 20 并发 P95=14ms | 佐证：瓶颈在环境，不在缓存链路 |

> 结论：2026-08-25 报告的分接口高 P95 是**本机 JMeter 与应用/数据库共享 CPU + 同机其他 Java 服务（newagent）**共同污染的结果；**Feed 快路径 + 覆盖索引架构在目标量级本身是合格的**。

### 3.4 混合档位对照（100 / 250 并发，60s，含 newagent 干扰）

| 档位 | 接口 | RPS | P50 | P95 | P99 | Avg | 错误 |
|---|---|---:|---:|---:|---:|---:|---:|
| 100 (50/30/20) | Feed | 521 | 45ms | **125ms** | 178ms | 53ms | 0 |
| 100 | 详情 | 1,469 | 26ms | **81ms** | 115ms | 31ms | 0 |
| 100 | 通知 | 376 | 41ms | **122ms** | 171ms | 49ms | 0 |
| 250 (125/75/50) | Feed | 561 | 97ms | **241ms** | 404ms | 112ms | 0 |
| 250 | 详情 | 2,041 | 32ms | **155ms** | 222ms | 51ms | 0 |
| 250 | 通知 | 389 | 92ms | **242ms** | 398ms | 107ms | 0 |

> 混合档 P95 显著高于单接口基线（详情 14→155ms、Feed 58→241ms），但吞吐接近饱和（Feed 100→250 档仅 521→561 RPS），0 错误、未崩溃、曲线线性。

## 6. 环境与后续建议

1. **正式容量验收前，必须清理同机干扰进程**（尤其 newagent 8081），否则 100/250 混合档数据仍会失真。
2. 下一步：干净环境下跑混合 100/250 并发（Detail/Feed/通知 = 50/30/20、125/75/50），对照规划验收标准（100 档每接口 P95<60ms、250 档<100ms）。
3. 本轮 JTL 原始数据保留于 `target/jmeter-results/feed-single-*`、`detail-single-*`、`notify-single-*`、`mix-100c-*`、`mix-250c-*`（2026-08-26）。

## 7. 混合档位严格验收结论

对照规划验收标准（100 档每接口 P95<60ms、250 档每接口 P95<100ms）：

- 100 档：Feed 125ms / 详情 81ms / 通知 122ms —— **未达标（差 1.3~2 倍）**
- 250 档：Feed 241ms / 详情 155ms / 通知 242ms —— **未达标（差 1.5~2.4 倍）**
- 但全程 0 业务错误、未崩溃、吞吐线性可预期

**归因**：单接口基线证明 SQL 层健康（Feed 20 并发 P95=58ms、详情 14ms、通知 26ms），混合档 P95 升高全部来自**同机资源竞争与排队**（本机 JMeter 与应用/DB 共享 CPU + newagent 8081 干扰 + 连接池竞争）。**当前环境的瓶颈是资源，不是 SQL**。

**达到严格验收的前提**：停掉 newagent、将 JMeter 移至独立压测机（或应用与 DB 分机）。此条件下 Feed/通知单接口 P95 有实测基线（58/26ms@20 并发）作为可达性参考。

---

*Git revision：待压测后统一记录*
*执行：灵犀 Agent（2026-08-26 10:24~10:33 CST）*
